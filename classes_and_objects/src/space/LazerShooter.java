/*
    INTERFACE CLASS

    (an abstract class?)
    Every interface class must include a 
    signature method (but not methods)
*/

package classes_and_objects.src.space;

// Interface class
public interface LazerShooter 
{

    void practiceShoot(); // Interface method (does not have a body)
}
